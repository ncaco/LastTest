import React from 'react';

const ServerImplementation = () => {
  return (
    <div>
      <h2>서버 프로그램 구현</h2>
      <p><b>[핵심] 백엔드 로직 개발:</b> 비즈니스 로직, 데이터베이스 연동, API 서버 구축.</p>
      <p><b>[중요] 서버 프레임워크:</b> Spring(Java), Node.js(Express), Django(Python), Flask(Python), Ruby on Rails.</p>
      <p><b>[시험] 배포 및 운영:</b> 클라우드(AWS, Azure, GCP), 컨테이너(Docker), 오케스트레이션(Kubernetes).</p>
      <p><b>[추가] 데이터베이스 연동:</b> JDBC, JPA, ORM.</p>
      <p><b>[예시] MVC 패턴:</b> Model(데이터), View(화면), Controller(제어) 분리.</p>
      <p><b>[예시] API 구축:</b> RESTful API 설계 및 구현, 데이터 형식(JSON) 활용.</p>
    </div>
  );
};

export default ServerImplementation; 